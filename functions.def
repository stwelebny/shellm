[
{
    "name": "execute_linux_command",
    "description": "Execute a generic Linux command",
    "parameters": {
        "type": "object",
        "properties": {
            "command": {
                "type": "string",
                "description": "The Linux command to execute"
            }
        },
        "required": ["command"]
    }
},
{
  "name": "self_call",
  "description": "Allows ChatGPT to call itself for multi-step tasks",
  "parameters": {
    "type": "object",
    "properties": {
      "task": {
        "type": "string",
        "description": "The task that ChatGPT should perform in the next step"
      }
    },
    "required": ["task"]
  }
}
,
]
